version: 0.1
python: 3.6
entrypoint: python runway_model.py
cuda: 10.2
spec:
    cpu: False
    gpu: True
files:
    ignore:
        - my_dataset/*
        - secrets.txt
build_steps:
    - pip install -r requirements.txt
